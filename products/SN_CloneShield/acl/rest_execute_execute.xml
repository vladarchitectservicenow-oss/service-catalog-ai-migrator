<?xml version="1.0" encoding="UTF-8"?>
<sys_security_acl>
  <name>x_snc_cs_execute</name>
  <type>rest_endpoint</type>
  <operation>execute</operation>
  <description>POST /api/x_snc_cs/execute — restore requires user, export requires user, snapshot requires admin</description>
  <active>true</active>
  <script><![CDATA[
var body = request.body.data;
var action = body.action || '';

if (action === 'snapshot') {
    answer = gs.hasRole('x_snc_cs.admin') || gs.hasRole('admin');
} else {
    answer = gs.hasRole('x_snc_cs.user') || gs.hasRole('admin');
}
]]></script>
  <condition/>
  <requires_roles/>
</sys_security_acl>
